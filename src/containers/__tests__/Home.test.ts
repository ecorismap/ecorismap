describe('', () => {
  test('', () => {
    expect(true).toBe(true);
  });
});

describe('データの追加', () => {
  test('ログインしていてプロジェクトに入っていなくてもデータ追加できる？', () => {
    expect(true).toBe(true);
  });
});

describe('ログアウト', () => {
  test('スマホなら単純にログアウト', () => {});
  test('Webならローカルキーを削除する', () => {});
});
