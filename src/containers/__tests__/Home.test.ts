describe('', () => {
  test('', () => {
    expect(true).toBe(false);
  });
});

describe('データの追加', () => {
  test('ログインしていてプロジェクトに入っていなくてもデータ追加できる？', () => {
    expect(true).toBe(false);
  });
});

describe('ログアウト', () => {
  test('スマホなら単純にログアウト', () => {});
  test('Webならローカルキーを削除する', () => {});
});
