# 立体図（標高タイルからの全方向陰影）の使い方

標高タイルから地形の陰影を計算して表示する機能です。

従来の陰影起伏図は光源方位に依存するため、地図を180度回すと凹凸が反転して見えました。
また光源と平行な走向の谷は明暗差を持たず、構造的に見落とされる弱点もありました。
現在の方式は式に光源が現れないため、**地図をどの向きに回しても見え方が変わりません**。

## 使い方

地図設定画面の URL に `relief://` を付けて標高タイルの URL を指定します。

```
relief://https://tiles.gsj.jp/tiles/elev/land/{z}/{y}/{x}.png
```

- 濃さは透過スライダーで調整します
- 「固定ズーム」は標高タイルが実在する最大ズームに合わせてください（産総研のタイルは 15）

旧来の `hillshade://` も引き続き受け付けます。既に追加済みの地図や共有プロジェクトの
設定を壊さないための互換で、動作は `relief://` と同じです。

## 対応する標高タイル

**国土地理院・産総研方式のみ**に対応しています。

```
x = 2^16·R + 2^8·G + B
NoData は 2^23（透明にする）
標高[m] = x < 2^23 ? x/100 : (x − 2^24)/100
```

Mapbox Terrain-RGB や Terrarium 形式には対応していません。

## 陰影の計算方法

MPI赤色立体地図（Kaneda and Chiba 2019）を無彩色化したもので、
**2つの「暗さ」の掛け算**です。

```
MPI  = 各方位(8本)について半径16px以内の最大仰角を求め、その平均[度]
傾斜 = 中央差分による勾配の大きさ[度]

s = clamp(傾斜 / 55°)
m = clamp(MPI / 25°) ^ γ        （γ = 1）
明度 = 255 × (1 − s) × (1 − m)
```

MPI（Morphometric Protection Index、保護指数）は「周囲の地形にどれだけ囲まれているか」
を表します。谷底は周りが高いので各方位の仰角が大きく MPI が大きくなり、
尾根や平坦地は周りが低いか同じなのでほぼ 0 か負になります。

| 地形 | 見え方 |
|---|---|
| 急峻な谷 | 両方効いて最も暗い |
| 急な尾根 | 傾斜だけ効いて中間 |
| 平坦な谷底 | MPIだけ効いて中間 |
| 平坦な尾根・台地 | どちらも効かず白 |

探索半径は画素固定の 16px です。z=14（約 9m/px）で約 145m となり、
MPI赤色立体地図で推奨される実距離 150m とほぼ一致します。画素固定なので
1タイルあたりの計算コストがズームによらず一定になります。

## 実装

| | |
|---|---|
| 計算 | `src/utils/terrainShading.ts` |
| Web のタイル生成 | `src/utils/shadingTileProtocol.web.ts`（maplibre の `addProtocol`） |
| 内部プロトコル | `terrainshade://` |

Web では maplibre 内蔵の hillshade レイヤは使いません（光源方位に依存するため）。
標高タイルを自前で取得・デコードし、陰影を計算した通常のラスタタイルとして返します。

- 探索半径のぶんの袖が要るので、1タイル描くのに周囲8タイルの標高を参照します。
  デコード済みの標高を128枚までキャッシュするので、実質1枚あたり1回の取得で済みます
- NoData と取得失敗は透明にします

オフライン用にダウンロードされるのは**陰影の計算結果ではなく生の標高タイル**です。
表示のたびに計算します。

## 注意

- 平坦地は白になります。乗算で重ねる前提の図なので、ベースマップに重ねるより
  単体（透過 0）で背景として使う方が本来の姿です
- ネイティブ（iOS/Android）は未対応です

## 参考

Kaneda, H., and T. Chiba (2019), Stereopaired morphometric protection index red relief
image maps (Stereo MPI-RRIMs): effective visualization of high-resolution digital elevation
models for interpreting and mapping small tectonic geomorphic features,
Bull. Seismol. Soc. Am., 109, 99–109. https://doi.org/10.1785/0120180166

手法は同論文に基づく独自実装で、QGISプラグイン（GPLv3）のコードは使用していません。
